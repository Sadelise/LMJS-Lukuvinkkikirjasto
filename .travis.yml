language: java

dist: trusty
addons:
  chrome: stable
before_install:
  - # start your web application and listen on `localhost`
  - google-chrome-stable --headless --disable-gpu http://localhost:8080

before_script:
  - chmod +x chromedriver.exe